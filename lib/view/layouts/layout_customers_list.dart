import 'package:flutter/material.dart';

class LayoutCustomersList extends StatelessWidget {
  const LayoutCustomersList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
