import 'package:flutter/material.dart';

class LayoutCategoriesList extends StatelessWidget {
  const LayoutCategoriesList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey,
    );
  }
}
