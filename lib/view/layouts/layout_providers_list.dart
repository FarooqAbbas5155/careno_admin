import 'package:flutter/material.dart';

class LayoutProvidersList extends StatelessWidget {
  const LayoutProvidersList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
